"""Fancy demo app package (RF-1 split of code/fancy_demo.py).

Submodules: constants, sampling, sampling_long, overlays_projection,
overlays_bev, overlays_ego, hud, cards, rollout, multi_goal, video, live,
web_html, web, cli.
The old top-level path `code/fancy_demo.py` is now a thin CLI entry shim
that re-exports this package's public surface (see code/fancy_demo.py's
own docstring).
"""
