"""REPL demo app package (RF-1 split of code/demo.py).

Submodules: constants, planner, maneuver_inferencer, executor, web, cli.
The old top-level path `code/demo.py` is now a thin CLI entry shim that
re-exports this package's public surface (see code/demo.py's own docstring).
"""
