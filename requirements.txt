# Python 3.10 environment used for development (NVIDIA GB10 / CUDA 12.8).
# NOTE: install torch + flash-attn + the gr00t package separately (see README):
#   pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu128
#   pip install flash-attn==2.7.4.post1                 # x86_64 (aarch64: prebuilt wheel in the GR00T repo)
#   pip install -e third_party/Isaac-GR00T              # the `gr00t` package (N1.6.1 release tag)

# --- GR00T model stack (pinned to match the N1.6 checkpoint) ---
transformers==4.51.3
diffusers==0.35.1
peft==0.17.1
accelerate>=1.0
safetensors==0.8.0
tokenizers==0.21.4
huggingface-hub==0.36.2
hf-xet==1.5.1
numpy==1.26.4
scipy==1.15.3
datasets==3.6.0

# --- Simulation / perception / ONNX teacher ---
mujoco==3.9.0
onnx==1.22.0
onnxruntime            # runs the WBC teacher ONNX at data-gen time (onnxruntime-gpu also fine)
opencv-python-headless==4.11.0.86
imageio==2.37.3
imageio-ffmpeg==0.6.0
albumentations==1.4.18
gymnasium==1.2.2

# --- Data / utils / demo UI ---
pandas==2.2.3
omegaconf==2.3.0
tyro==0.9.17
wandb==0.23.0
flask                  # interactive demo web UI (demo.py / fancy_demo.py)
